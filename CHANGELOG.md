# 更新日志 Changelog

版本以 GitHub Release 标签管理，**与应用（客户端）版本号保持一致**。格式参考 [Keep a Changelog](https://keepachangelog.com/)。

---

## v2.4.0 — AI 探索与全场景阅读（2026-08-31）

### 订阅发现（全新）
- **AI 探索**：在订阅商店一键发现目录之外的高质量新源——候选经本地真实抓取验证后才展示（更新频率 / 单篇深度 / 最新 3 篇样章预览），AI 解释「为什么推荐给你」（基于本地兴趣画像归因）；探索风格三档（保守 / 平衡 / 大胆），「不感兴趣」自动记忆并写回画像；未配置 AI Key 时自动降级为基础版探索
- **订阅源自动发现**：添加订阅粘贴博客首页 / 域名即可，不再需要精确的 feed 地址
- **商店治理**：移除全部装饰性假数据（评分 / 订阅数），改为真实健康标注（抓取失败自动标灰沉底）；修复重复条目与专题漂移；新增 120 个中文独立博客源（中文源占比 21% → 52%）；免费版订阅上限改为对话框完整引导
- **稍后读队列**：与已读 / 收藏独立的第三状态——侧栏专用视图（带计数）、列表右键切换、阅读器头部按钮

### 阅读
- **TTS 听文章**：本地语音离线朗读，按句切块稳定播报，当前段落跟随高亮滚动；迷你播放器支持播放 / 暂停 / 语速 / 声音选择；R 键读停切换
- **Miniflux 同步**：支持 Miniflux 2.42+ 内置 Google Reader API（账号表单选服务类型即可），复用完整双向同步；修复两处 GReader 多账号场景的兼容问题
- 智能文件夹可用：「打开」即按关键词聚合文章进列表
- OPML 导入保留标题与文件夹层级

### 桌面与数据
- 系统托盘（显示 / 刷新 / 自启 / 退出）、关闭窗口最小化到托盘
- 刷新发现新文章时系统通知（仅后台时弹出，可关）
- 备份导出 / 恢复导入（单文件 JSON，含全部数据与偏好）；存储管理（体积统计 / 一键清理 / 打开数据目录）

### 界面
- 英文界面全量补齐（新增 635 条翻译，渲染层全部文案覆盖）
- 主题设计器与探索面板细节打磨

---

## v2.3.0 — 性能与阅读体验跃升（2026-08-31）

### 性能
- 正文提取迁移至独立进程：后台预抓取不再阻塞界面，抓取期间阅读与搜索保持流畅
- 打开文章 LRU 缓存 + 相邻文章预取：连续阅读（J/K、空格翻篇）瞬时呈现，不再出现加载遮罩
- 状态推送合并与增量更新：连读时未读计数即时刷新，长列表不再整表重拉
- 全文搜索升级 FTS5 索引（相关度排序 + 命中摘要），并首次覆盖 RSS 正文内容；短词自动回退模糊匹配

### 阅读
- 代码语法高亮（highlight.js 本地内置；语言标注精确着色，无标注代码块保守着色）
- 数学公式渲染（KaTeX 按需加载：`$$…$$`、`\[…\]`、`\(…\)`，普通文章零开销）
- 多图并排画廊行：相邻纯图片自动等高并排，灯箱与懒加载不受影响
- 订阅源正文格式自动规范化：Markdown、转义 HTML、纯文本源不再显示裸符号或标签文本
- 阅读位置跨会话记忆：重启应用后接着上次的位置继续读
- 列表排序切换：时间序 / 未读优先
- 导出三件套：复制全文 Markdown、导出 .md 文件、打印 / 存为 PDF
- 订阅图标本地三级缓存（内存 / 磁盘 / 网络），大陆网络下侧栏与列表图标不再反复失败
- X/Twitter 正文头像过滤；AIHOT 徽标与板块切换修复；主题设计器内存泄漏修复

### 修复
- 修复 13 项缺陷：搜索回退报错、FreshRSS 离线已读状态回滚、AI 摘要卡重复触发、
  慢文章加载竞态、AI 流式请求无超时挂死、对话框 ESC 挂起、批注高亮越界、
  Markdown 链接属性注入（安全）、订阅重复入库、退出丢失偏好设置、
  孤儿数据不清理、限流源重试策略、预发布版本比较误判等
- 界面渲染进程启用沙箱（安全加固）

---

## v2.2.0 — 开源基线（2026-08-29）

仓库首个 Release，作为后续版本管理的基线；与客户端构建 v2.2.0 同版本号。发布页附 `RobinRead-2.2.0-setup.exe` / `RobinRead-2.2.0-portable.exe` 及 SHA-256 校验值。

相对 v2.1.0 的增量：官网 `update.json` 更新源接入（应用内检查更新）、会员 / 激活码体系上线（免费 30 源 · 每日 3 次 AI；月卡 / 终身）。

### 阅读
- 纸感三栏布局（订阅 / 列表 / 阅读），衬线排版、纸纹噪点，明暗双套主题
- 章节导航轨道（TOC Rail）、浮动滚动条、空格翻篇、禅模式
- 阅读位置记忆（切换文章后回到原位置）
- Mozilla Readability 通用正文提取 + HTML 白名单消毒，烂排版一键重排
- 图片加载占位与失败兜底；微信文章图片直连源站（绕过代理，注入 Referer 防盗链）
- 聚合器页面内容治理：剥离导航残留与元信息尾巴，Hacker News 源转为友好卡片

### AI
- 流式全文摘要（折叠卡片）
- 逐句对照翻译（原文/译文并排）
- 划词解释、翻译、多轮追问
- 今日 AI 简报（按主题分组 + 值得深读推荐）
- AIHOT 热点榜 / AI 日报 / 精选 / 大模型综合榜；刷新时后台预抓取，打开秒开
- 模型接入：DeepSeek / OpenAI 兼容 API / 可信局域网 HTTP 服务

### 批注与知识
- 五色高亮（黄/绿/蓝/粉/紫）、段落便签、批注总览面板（可折叠、可回锚）
- `H` 键快速高亮
- 知识中心：标签云、看板、复习、收藏集、热力图、导出
- 阅读进化统计

### 订阅
- 本地账户 + FreshRSS（Google Reader API）多账户，未读/星标双向同步、离线变更队列
- 订阅商店：190+ 人工核验精选源、12 分类、编辑精选一键订阅
- OPML 导入导出、ETag 条件刷新
- 订阅去重与数据自修复（链接实体解码规范化、缓存自愈）

### 账号与会员
- 用户名密码登录（JWT，30 天），激活码兑换会员（月卡 / 终身，后端验证）
- 凭据 DPAPI 加密存储；AI API Key 仅存本机

### 工程
- Electron 37 + `node:sqlite` 本地优先存储，三级遗留数据迁移（目录 / 偏好 / LocalStorage）
- 中英双语界面；OKLCH 主题引擎 + 主题设计器（传统色、色觉模拟、WCAG 对比度、令牌导入导出）
- 应用内更新检查（官网 `update.json` 更新源）
- 测试脚本：`scripts/selftest.js`、`scripts/uitest.js`、`scripts/verify-realrun.js` 及各专项验证脚本

---

## v2.1.0（2026-08-19）

- 引入 Mozilla Readability 通用正文提取（替代逐源特判），可绕过反爬壳页
- 修复微信文章链接实体解码导致的跳转失败、AIHOT 聚合页内容缺失、中文订阅重复入库
- 微信图片绕过 img-proxy 直连源站，加载速度大幅提升
- 阅读体验优化：图片加载占位 / 失败兜底、阅读位置记忆、字体栈调整

## v2.0.0（2026-08-19）

- 「知更」视觉体系确立：纸感三栏、衬线排版、OKLCH 主题引擎与主题设计器
- 数据升级机制：数据目录 / 偏好键 / LocalStorage 三级自动迁移，升级不覆盖新数据
- 批注系统（五色高亮 / 段落便签 / 批注总览面板）、阅读与研究面板折叠
